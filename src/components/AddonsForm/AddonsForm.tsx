import { UserInfoTypes } from "@/utils/types";

interface Props {
  setUserInfo: any;
  userInfo: UserInfoTypes;
}

const AddonsForm = (props: Props) => {
  return <div>AddonsForm</div>;
};

export default AddonsForm;
